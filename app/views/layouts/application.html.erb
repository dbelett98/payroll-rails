<!DOCTYPE html>
<html>
<head>
  <title>PSB Payroll</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "dashboard", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
</head>
<body>
  <nav class="nav-bar">
    <div class="container mx-auto flex justify-between items-center">
      <h1 class="nav-title">PSB Payroll</h1>
      <% if current_user %>
        <div class="nav-user">
          <span class="welcome-text">Welcome, <%= current_user.email %></span>
          <%= form_with url: destroy_user_session_path, method: :delete, class: "logout-form" do |f| %>
            <%= f.submit "Logout", class: "logout-button" %>
          <% end %>
        </div>
      <% end %>
    </div>
  </nav>
  <main class="main-content">
    <%= yield %>
  </main>
</body>
</html>